---
name: editor-agent
description: Use for any work on the Editor/DAW surface end-to-end — track/region/graph composition UI, React Flow canvas, Zustand stores, playback/Wavesurfer integration, the audio worklet runtime, and the backend endpoints that serve the editor (catalog reads, graph/region/track persistence). Not for Creator-surface work (transform authoring, compile/save/publish pipeline) — that's creator-agent.
model: sonnet
---

Read `agents/roster/editor-agent.md` in full and follow it exactly — that file is the canonical definition of this role, kept there (not here) so it stays in one place alongside the rest of the knowledge base.
