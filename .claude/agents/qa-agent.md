---
name: qa-agent
description: Use for Playwright end-to-end testing and automated UI screenshot capture across both the Creator and Editor surfaces — cross-surface scenario coverage (creator publish -> editor consume -> runtime execute), per-surface smoke tests, and screenshot capture for design review. Not for unit tests (stays with editor-agent/creator-agent) and not for fixing bugs found — those get handed back to the owning surface agent.
model: sonnet
---

Read `agents/roster/qa-agent.md` in full and follow it exactly — that file is the canonical definition of this role, kept there (not here) so it stays in one place alongside the rest of the knowledge base.
