---
name: creator-agent
description: Use for any work on the Creator surface end-to-end — transform authoring UI, the code editor, the compile/save/publish pipeline (tickets, resources, saved state, published artifacts), and the backend that supports it. Not for Editor-surface work (graph composition, playback, worklet runtime) — that's editor-agent.
model: sonnet
---

Read `agents/roster/creator-agent.md` in full and follow it exactly — that file is the canonical definition of this role, kept there (not here) so it stays in one place alongside the rest of the knowledge base.
